# 实际测试记录

环境：Windows，Godot 4.6.stable.official.89cea1439，Compatibility 渲染。运行日期：2026-09-08。结果 JSON 由 tests/test_suite.gd 生成。

- 8282 检查，0 失败。450 个 seed/桌面高度组合覆盖确定性、数据合法性、边界和保守碰撞间距。
- 实际推进 Godot 物理：重力、地板停留、挡板改变水平轨迹、球间动量传递、磁力加速、风扇逆重力推动、旋转风向、对称磁场抵消、弹簧阻尼振荡。
- 模拟 ScreenTouch/ScreenDrag 进入同一 GUI 输入路径，验证工具栏拖出、单指所有权、暂停摆放、异常触点释放速度上限。未冒充电容屏实测。
- 删除连接级联清理，复制、清空、删除、撤销、恢复图关系、暂停与继续。
- 临时文件写入、替换、损坏主存档回退备份、拒绝非法属性及未知版本、组合前快照落盘、作品保存与打开。
- 显式发送后台/前台通知验证冻结与恢复状态。另用两个独立 Godot 进程执行 restart.gd 的 write/read，验证启动时读取道具、连接、暂停和偏好。真实 OS 杀进程行为仍待手机验证。
- 48 球 + 5 固定道具 + 8 弹簧运行 1800 物理步，每 60 步抽样检查边界/有限状态；测得抽样最大速度约 430.40 px/s，低于 1100 限制。测试不等价于所有可能搭建的稳定性证明。
- 450×900、450×800、450×1000 的真实渲染截图已检查，设置浮层已检查。布局随宽高变化，最终安全区域依赖平台 API，刘海/导航栏必须另做真机测试。

命令：Test-PocketLab.ps1；测试数据单独写入 user://tests。无头测试通过不代表真实手机帧率、功耗、触感或音质合格。详细真机清单见 MOBILE.md。


## 第二版 0.3.0

revision_suite.gd 已更新为实时编辑交互的回归，共 157 项、0 失败。测试实际 Viewport 长按/滑块事件、无点选浮动栏、一次编辑一次撤销、快照还原、拒绝重叠增长、拖动停顿不触发面板、方向/风力实时生效、复制、风扇静音、UI 静音、材质波形区别、合成传感器死区/限幅/冷却/重置、无设备降级、后台恢复、不同质量的风场运动差异、下滑关闭以及不同窗口高度下完整画布可见性。旧版草稿提交和旋转把手交互不再适用。

导出 PocketLab-v0.3.pck 后，使用 Godot --main-pack 启动 120 帧无错误。PCK 为资源包；尚无 APK 或手机性能结论。传感器测试使用合成输入，真机握持轴向、环境振动与灵敏度待测。
